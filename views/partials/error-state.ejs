<section class="notice error">
  <span class="notice-kicker">Attention needed</span>
  <strong><%= message %></strong>
  <% if (typeof detail !== 'undefined' && detail) { %><p><%= detail %></p><% } %>
</section>
