<section class="notice empty">
  <span class="notice-kicker">Setup / empty state</span>
  <strong><%= message %></strong>
  <% if (typeof detail !== 'undefined' && detail) { %><p><%= detail %></p><% } %>
</section>
